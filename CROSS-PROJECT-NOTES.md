# 横展開メモ — Twitter学習 → Taste Spoon / Taste Glass への気づき

Twitterクローン学習で出てきた、他プロジェクトに応用できそうな気づきを残す。
**ここでは実装しない。** 気づき・対応づけだけメモする。

## 気づき
<!-- 例（種）:
- RLS「自分のデータは自分のもの／他人は消せない」は、電脳タグ社会レイヤーの設計原則
  「自分のレイヤーは自分が所有、他人は同意なしにあなたをタグれない」に直結。
  → 電脳名刺の所有モデルを Supabase RLS の発想で説明できるかも。
-->
- 【RLS = 所有モデル】RLS「自分の行は自分だけ読む/直す/消す（auth.uid()=user_id）、他人は触れない」は、電脳タグ/電脳名刺の社会レイヤー設計「自分のレイヤーは自分が所有、他人は同意なしにタグれない」とそっくり。所有を“持ち主ID”で表し、許可ルールで分離する発想がそのまま使える。
- 【止めるのはDB側】「フロントで隠すだけは穴、APIを直接叩かれたら漏れる。強制はサーバー/DB側」＝電脳タグでも「見た目で出さない」では不十分で、消さない設計でも“誰が何を触れるか”はサーバー側で担保が要る、という示唆。
- 【auth.uid()は偽れない】持ち主判定はクライアント申告ではなくサーバーが知るIDで行う＝なりすまし防止。正直メモ/信頼の浮き沈み（パンマン集合版）でも「発言主は偽れない」土台に同じ考え方。
- 【RLS無しDBとの対比】MySQLや素のPostgreSQLはアプリ側で毎回 where user_id を書く＝1か所の付け忘れが穴。所有を仕組みで強制できるか（RLS）/手作業か、の設計判断は味覚データの共有範囲設計でも効く。

# 今日の学びメモ — Twitterクローン学習（2026-06-27 もくもく会）

各フェーズ終了時に「今日の学び」を1〜2行で追記する。最後に下の「結果報告」へまとめる。

## ログ
<!-- 例: - (Phase 0) Supabaseは「サーバー＋DB＋認証」を1つにまとめたサービス。最初はHTML1枚で十分動く。 -->
- (Phase 0) Supabaseは「サーバー＋DB＋認証」を1つにまとめたサービス。HTML1枚＋anon keyで動かせる。鍵は2種類＝anon/publishable(ブラウザOK・RLSで守る前提)とservice_role/secret(マスターキー・絶対出さない)。Google認証は「アプリがGoogleに本人確認を頼む」仕組み(OAuth)で、戻り先URL(リダイレクトURI)はGoogle側=Supabaseのcallback、Supabase側=自分のアプリ(localhost:8000)の2種類を登録する。
- (Phase 1) 登場人物＝ブラウザ(お客)⇄supabase-js(窓口/API)⇄Supabase(裏方:Auth/DB)。「誰のものか」を裏方が判断する仕組みがRLS(第4章の本命)。
- (Phase 2) signInWithOAuth({provider:'google', options:{redirectTo: window.location.origin}}) でGoogleログイン起動。HTMLはfile://だと動かず、http://localhost:8000(python3 -m http.server)で配信する必要がある（importやリダイレクトがブロックされるから）。ハマり: 「Unable to exchange external code」はSupabaseに貼ったGoogle Client ID/Secretの不一致が原因→Google認証情報からコピーボタンで貼り直して解決。コードを貼る場所ミス（外枠の中に外枠を二重に貼って});が余る）も体験。
- (Phase 3) tweets表をSQL Editorで作成(id自動/body/created_at自動)。保存=insert({body})、表示=select('*').order('created_at',{ascending:false})で新しい順。id/created_atは「自動」にしたのでDBが付ける→bodyだけ渡せばよい。{body: body}は左=列名・右=入力変数。観察: 「未ログイン」のまま投稿できた＝RLS無し＝無防備な状態。これが第4章の出発点。Supabaseもテーブル作成時に「RLS無し」警告を出した(Run without RLSを選んだ)。
- (Phase 4 / RLS) ①user_id列(uuid, references auth.users(id), default auth.uid()=投稿者を自動で持ち主に) ②alter table ... enable row level security で鍵 ③4つのpolicy(select/insert/update/delete を auth.uid()=user_id で自分の行だけ)。体験: RLS有効化直後ポリシー無し→insertが "new row violates row-level security policy" で全ブロック→ポリシー配布→ログインして投稿成功。マジック: アプリは select('*') のまま(「自分のだけ」と書いてない)のにRLSが他人の分を弾く＝DB側で強制が最強。auth.uid()はDBが知るので偽れない。フロントで隠すだけは穴(APIを直接叩かれる)、止めるのはDB側。
- (第6章 公開タイムライン) is_public(boolean default true)列を足し、読むポリシーを drop→ using(is_public or auth.uid()=user_id) に作り直すだけで「みんなの公開 ＋ 自分の下書き」を両立。insert/update/deleteは第4章のまま。アプリ側は select('*') 不変、削除ボタンは t.user_id===myUserId のときだけ表示(見た目のおまけ、真の防御はRLS)。投稿に is_public:false(下書きチェック)で自分だけに。確認: 隠れてた他人公開ツイートが見えるように/下書きはログアウトで消え公開は残る。
- (気づき: DBが本体/エディタは窓口) SQLエディタは命令を送る窓口で、結果はDB側に永続。だから毎回前のSQLを消して新しい命令を送る(同じcreateを二度でalready existsエラー)。「保存して積み上げたい」直感＝実務のmigrations(001_*.sql…と順序保存して再現/履歴。Supabaseにも有り)。
- (第7章 フォロー) 人と人のつながり=多対多。ユーザー行にリストを書くのは無理(相手は0〜数千)→「フォロー関係だけの専用表(中間テーブル)follows」1行=1関係。follower(する人)→followee(される人)の向き。複合主キー(follower_id,followee_id)で二重フォロー防止、check(follower_id<>followee_id)で自分フォロー禁止、references=外部キー。RLS: 読むはusing(true)公開、作る/消すはauth.uid()=follower_idで自分の分だけ。アプリはfollowee_idだけ渡せばfollower_idは自動。実体験: 未ログインでフォロー→RLSで弾かれた(誰でもないのにフォロー不可)。
- (第8章 ホームタイムライン) SQL変更なし、2つの表をまたぐ「2段がまえ」=①follows から followee_id 一覧(友だちリスト)②tweets を .in('user_id', ids) でしぼる。= 結合/サブクエリの入口。公開タイムライン(全世界)とホーム(フォロー中だけ)を同じDBから作り分け。follows の select は using(true) なので自分の分は .eq('follower_id', myUserId) で絞る。宿題: フォロー増で .in が重い→view/RPC(第9,12章)、N+1問題。
- (作り手の学び) 機能の本質を先に・デザインは後で。今日は見た目ゼロのままRLS/フォロー/ホームまで本質を動かし「データは誰のもの？」を最速で検証できた。順番＝「まず効くか→次に気持ちよさ」。MVPの考え方そのもの（Taste Spoon/Glassにも同じ）。
- (migrations実践) 今日のSQLを migrations/001〜004 + README に整理。空DBに順に流すと同じ構造が再現＝「どこでも建て直せるレシピ」。再現するのは構造(スキーマ)だけ、データ(中身)は別。DBの構造がコードになる＝履歴管理・再現。
- (第9章 キャッシュ) キャッシュ=近くに置く「最新版のコピー1個」で速くする工夫。履歴(過去ぜんぶ)とは別物=最新のつもりの1個だけ持ち上書きされる。DB(倉庫/正解)・キャッシュ(近くの控え)・履歴(変化の記録)は別。2リスク=①古くなる②他人の分が共有キャッシュに残る(事故)。公開データ=public(共有OK)/個人データ(ホームTL・下書き)=private必須、CDN厳禁。ログアウトでもブラウザの控えは消えない。直したのに直らない時はまずキャッシュを疑う(スーパーリロード)。
- (第10章 ありがちな失敗/点検) 流出の大半は派手なハッキングでなく設定ミス(玄関のカギ閉め忘れ)。8項目で自分のアプリを点検→ほぼ合格: RLS有効○/削除ボタンはifで隠すがRLSが本体○/置いてるのはanon keyだけ(service_role無し)○/textContentでXSS回避○(innerHTMLだったら危険)/CDN無し○/.eq().in()で安全○/N+1無し○。軽微: tweet.idは連番だがuser_idはUUID＆守りはRLSなので実害小。教訓: 守りの本体は常にサーバー側(RLS/権限)、画面の隠し・IDの当てにくさはおまけ。
- (第11章 Vibe coding) 「動く」と「安全・正しい」は別物。AIは速い/雛形は得意だが、RLS忘れ・秘密鍵をフロント・危ない近道が苦手→人間に「読める目」が要る。うまく作らせる4コツ=①一気に作らせず1機能ずつ②前提明示(Supabase+Google+RLS)③なぜも説明させる④動かして確かめる。今日のセッション自体がこの型の実演だった。AIは自信満々に間違える(ハルシネーション)ので最終判断は人間＝チェックリストと照らして自分で納得。
- (第12章 スケール) 規模が変われば設計も変わる。pull型(fan-out on read=読むとき集める/今日のホーム)はフォロー多いと読みが重い、push型(fan-out on write=書くとき受信箱に配る)はフォロワー多いと書きが重い=重さの場所が逆。Twitterの実話Fail Whale→受信箱(タイムラインのキャッシュ/Redis)→ハイブリッド(ふつうの人=push、セレブ=pullで読むとき混ぜる)で良いとこ取り。学び: ①正解は1つでない(規模で変わる)②最初から1億人向けに作らない=早すぎる最適化、まず素直に作り詰まってから直す③単純なお題に深い設計④小さなTwitterも巨大Twitterと地続き。★教材 全12章＋付録 完走。
- (作り手の学び/AIとの進め方) AIに新機能を相談すると“どんどん作って”しまう。必ず ①ゴール設定 ②フェーズ分け ③どんな機能・選択肢があるか(GitHub等の既存も)を整理 してから進めると、自分の理想に近いものが作れる。実装に走らせず手綱を握るのは自分。「自動生成アバター」より「各自が好きな画像を登録」が欲しい、のように“何が欲しいか”を先に言語化するのが大事。
- (X4a アバター) DiceBear(OSSの自動生成アバター, GitHubのidenticonと同型)を <img src=".../identicon/svg?seed=user_id"> で表示。画像を保存せずIDから毎回同じ絵を生成＝アップロード/ストレージ不要。タイムライン・ホーム両方に。外部の生成サービスをimgで使う発想。
- (X4b 一覧からフォロー) UID貼り付けを撤去。profiles全件＋自分のfollowsを突き合わせて「フォロー/フォロー中」を出し分ける一覧に。注: プロフィール登録済みユーザーしか並ばない。ホームに全員一覧は本物Xの形ではない(X=プロフィール/検索からフォロー)→画面分けは後日。
- (A アバター画像アップロード/A1-A3) Storage(ファイル置き場, DB＝表とは別)に公開バケットavatars作成。読むは公開だが書き込みはRLSポリシー(authenticatedのinsert)が要る。流れ=upload→getPublicUrl→profiles.avatar_urlに保存→<img>表示。表示はavatar_url優先・無ければ自動生成のフォールバック。標準＝Supabase公式のアバターupload手順と一致(GitHub/公式を確認してから実装)。ハマり: bucket名を'???'のまま実行→not found／変更後はスーパーリロード必須(キャッシュ)。
- (P プロフィールまとめ) Supabase公式Accountパターンに倣い、表示名・ひとこと(bio)・アバターを1枚の「プロフィールカード」に集約。bio列追加→update({display_name, bio})。標準(GitHub/公式)を見てから実装。次の改善＝編集は専用「Edit profile」画面で。
- (総括/今日いちばんの学び) ①最初に「作るもの全体のゴール設計」をするのが大事。②フェーズに分けて進める。③デザインから入らず、世の中(GitHub)に大量にある事例を見て、作りたいものに合えばそれをベースに機能を実装する方が「理想とブレない」。④作ると小さな所が気になる→都度直す。⑤全部AIに任せず自分も手を動かす方が安定（途中で動かない/最終的に変なものができる、を防げた）。⑥今後の自分のプロジェクトの新機能も、世の中の基本アイデアをベースに広げる。⑦デザインより「コア機能＝ユーザーが喜ぶ機能」がちゃんと作れているか／セキュリティが担保されているか／使いにくくなっていないか(UX)を忘れずに作る。
- (デプロイ) GitHub Pagesで公開 → https://shoujiki-panman.github.io/minitwitter-x/ （repo: shoujiki-panman/minitwitter-x、index.htmlのみ公開）。公開URLでログインが戻るよう redirectTo を window.location.origin+pathname に変更。「シンギュラ限定」の作り＝ D3:Supabase URL Configに公開URL追加／D4:Google OAuthテストユーザーにメンバー追加(テスト状態のまま)／D5:RLSでtweets selectを to authenticated に＝未ログインは中身ゼロ。GitHub Pagesは無料枠だとURL自体は公開なので、ログイン＋RLSでガードする方式。
- (画面分け N1-N3＋UX) 1枚HTMLでハッシュ・ルーティング(#home/#explore/#profile, hashchangeで該当sectionだけ表示)＝標準SPA(GitHub例 managervcf/vanilla-js-single-page-app)。.view{display:none}を既定にしJSで現在ビューだけblock表示(CDN import読込中の漏れ防止)。★ハマり2回: style.display='' はCSSのnoneが残って隠れたまま→'block'/'inline-block'で上書き。ログインゲート(applyAuthGate): 未ログインは案内だけ・ログインでアプリ表示＋ログイン/ログアウトのボタン出し分け。下書きはホーム(.eq('is_public',true))から外しマイページに「自分の投稿(🔒下書き)」表示。ログアウト確認は<dialog>+showModal()の自作モーダル(confirm()はオリジン名見出し付き＆装飾不可だから)。
- (E1 検索) さがすに検索ボックス。Supabase標準=.ilike('列','%語%')(大小無視の部分一致)、複数列は.or('display_name.ilike.%q%,bio.ilike.%q%')。表示できるのはprofiles登録済みの人だけ＝「名無し」は描画時のフォールバックで実データではない→検索/一覧に出すにはプロフィール行が必要(本物Xは全員ログイン時に自動作成)。
- (Cf コメント機能) commentsテーブル(tweet_id→tweets on delete cascade, user_id→auth.users 既定auth.uid(), body)＝tweetsと同じ型を親投稿に紐付け。RLS読む公開/書く自分。各投稿に💬件数(fetchCommentCountsで1回集計=N+1回避)＋クリックでコメント一覧&入力をトグル表示。投稿行を.tweet-rowでラップしコメント欄を下に。標準=Supabase RLS/Twitterクローン例。
- (F フォロー解除) 既存のフォロートグルに confirm('フォロー解除しますか？') を追加(誤操作防止)。
- (デプロイ運用) Site URLを公開URLにすると、ログイン後は常にSite URLへ着地する。ローカルのredirectTo(localhost:8000/)がRedirect URLs(localhost:8000・末尾スラッシュ無し)と完全一致せずSite URL(公開)へフォールバック→localでテストしたいなら http://localhost:8000/** をRedirect URLsに追加。GitHub Pagesもキャッシュ強いのでpush後はハード再読み込み。
- (U1 他人プロフィール) ハッシュ・ルーティングを #user/<id> に拡張し view-user を表示=任意ユーザーのアバター/名前/bio＋フォロー解除ボタン＋公開投稿。アバターを avatarEl() でクリック可能に(押すと location.hash=#user/id)。これでフォロー解除も他人プロフィールから可能。
- (レスポンシブ) スマホで小さく縮む＝<meta name="viewport" content="width=device-width, initial-scale=1"> が無いとモバイルがPC幅(980px)で描画して縮小するのが原因。これを入れると実機幅で描画。max-width:600px中央寄せでPC/スマホ両対応(左サイドバーは別作業)。
- (デモ配慮/プライバシー) 自動表示名をメールの@前→「パン仲間+乱数」に(メール名の流出防止)。「自分のデータを消す」ボタン=likes/comments/follows/tweets/profilesを自分の分だけdelete→signOut。profilesにdeleteポリシー(auth.uid()=id)を追加して初めて自分のプロフィールを消せる(RLSは無いと削除も拒否)。tweets削除で紐づくlike/commentはcascadeで消える。
- (UI/モバイル) 投稿のアクション(いいね/コメント/削除)を本文の右→本文の下の行(.tweet-actions)に。本文は横幅いっぱいで折返し減=スマホで窮屈さ解消。本物Xと同じ「本文の下にアクション行」。

## 🧠 今回のバイブコーディングの学び（総括・本人の振り返り）

### 前半（教材どおり）と後半（自分の拡張）は分けて見る
- **第1〜12章は教材（説明書）どおりに1章ずつ**進めた。＝自分で考えたというより、教材に沿って理解しながら作った部分。
- **それ以降（いいね・コメント・プロフィール・検索・他人プロフィール・画面分け・スマホ対応・デモ配慮）は教材に無く、自分で「Xに近づけたい」と考えて作った拡張**。
- 後半の進め方＝いきなり「これとこれをやりたい」ではなく、**「こういうことを実装したい → 世の中(GitHub)からおすすめの標準を探して」** と指示し、**ゴールとフェーズに分けて、一気に勝手に進めさせない**ようにした。

### データは誰のもの？（なぜ大事か）
- Twitterのようなサービスで、**自分のコメントや下書きが誰にでも丸見え**だったり、**他人の投稿が見えたり消せたり**するのは問題。
- だから**データベースをちゃんと作ること・セキュリティをしっかりすること**が、サービスを運用していくうえで欠かせない。
- これは前半（教材のRLS）だけでなく、**後半の自分の拡張（コメント／下書きはマイページだけ／未ログインは中身が見えない 等）でも実際に効いた**。

### もともとの課題
- 非エンジニアだと、ざっくりした「やりたいこと」を投げると、理想と違うものができたり、曖昧なまま進めてしまう。結果、物事が進まずうまくいかない。
- UI・デザインをAIに指示するのが難しい。**AIが思う「いいデザイン」と、自分が思う「いいデザイン」に差がある**。
- AIは勝手に物事を進めたり、横に逸れたりする。
- 以前は複数のAIを併用（最初にClaudeで考え → MulmoClaudeで実装）していたが、**Claude側で「今なにが実装されているか」が見えづらく**、なかなか進まないことがあった。今回 **Claude Code に一元化**したら、非常に進みやすく・正確になった。

### うまくいった進め方（今回の結論）
- **コアとなる機能を決めて、ゴール設計とフェーズ分けをし、必要な機能を正確に実装していく**。→ 自分の学びにもなるし、AIを先生役にすると一緒に考えて進めてくれる。
- 一個一個の指示でもいいが、最初にゴール設計・フェーズ分け。**機能を実装するたびに一部は自分の手を動かす**。**プレビューで実際の画面を見ながら**進める。→ 大きな失敗が少なく、結果的に早い。
- **UI・デザインの指示は、GitHubの既存（標準）を参照させて、AIに分かりやすく指示する**。
- デザインから入る方法は及川さんが Claudeデザインで証明してくれた。参考にしたいが、基本は「コア機能を決めて、ゴール・フェーズで実装」が現時点では確実。
- Xで見るような「一撃で理想のものが出る」もあるが、**現時点ではこの方法が確実**。
- **メモリー機能も大事**。

### 有元さんの教え
- ターゲットを決める（例：非エンジニア向け）
- 実現したいミニマムのコア機能（＝バリュー）を決める
- 動くようになったら、**使いながら機能をまとめていく**のが基本
- MulmoClaude はこのやり方を取っている
- 骨格は X などを参考に
- 無理なことを言ってみると提案してくれる → 意外なものができるようになる

### 及川さんの教え
- Claudeデザインはクレジットを使うが、いいデザインを作ってくれる。デザインの具体は Claude が考えてくれる。

### デザインは「カタログから選ぶ」感覚で
- AIが作るデザインは「なんじゃこりゃ」というのが出る時がある。
- 及川さんの言う「Claudeデザイン」を使うのが良い。
- 的確にやるなら、GitHubで“すごい人が作ったデザイン・機能”を見て「これはいいな」と**カタログから選ぶ感覚**で進める方が、バイブコーディングとしてやりやすい。
- **最初からデザインにこだわりすぎると、AIのプラン（使用枠/クレジット）を使い切ってしまう**ことが多い。まず**コアを実装してから、デザインは後で肉付け**する方が、結果的に良いものができる。
- 一部 Claudeデザインを使えば、最初から理想のUIが安定して出る場合もある（今後やってみたい）。ただし**一撃で最初からいいものが出るケースは少ない**ので、ちょこちょこ少しずつ進める方が確実。

### この教材（有元さんのTwitter作成マニュアル）について — おすすめ
- バイブコーディングを始める人が**最初に着手する教材として本当に良い**。会社の研修などでも使っていきたい。
- ここから始めると「バイブコーディングが楽しい」と思えるし、**自分の成果として見えやすい**。
- まだやってない人はぜひ。土日に**早ければ2〜3時間、5時間くらい**でいいものができる。

> 実際に最初にClaudeへ渡したキックオフ指示は `START-HERE.md`（GitHubのREADMEにも掲載）。他テーマで使い回せる汎用版は `AIと作る進め方テンプレート.md`。
- (X2 デザイン) HTMLは変えず<style>だけで黒背景・青ピル・区切り線・中央寄せ＝X風に。「機能の後にデザイン」。メアドは個人情報なので画面に出さず @表示名 に変更(プライバシー)。
- (X3 いいね) likes中間テーブル(user_id+tweet_id, 複合主キーで二重防止, on delete cascade)＝フォローと同じ多対多の型。❤️トグル(insert/delete)、user_idは自動、RLSで自分のいいねだけ。件数は.in('tweet_id',ids)で1回集計＝N+1回避。実体験: いいねボタンが出ない→第9章のキャッシュ！通常更新は古い控えを再利用、スーパーリロード(Cmd+Shift+R)で控えを捨て最新取得して解決。本番はキャッシュバスティングで対策。
- (X1 プロフィール) profiles(id=auth.usersと1対1, display_name)＋RLS(読む公開/書くは自分)。ログイン時 upsert(ignoreDuplicates=無ければ作る・あれば触らない)で自動作成、表示名は update().eq('id',myUserId)。ツイートはUID→名前表示。名前は fetchNames が .in('id',ids) で1回まとめ取得＝N+1回避。未作成ユーザーは'名無し'フォールバック。
- (Phase 4 検証) 別人ユーザーを作りSQL Editor(RLS素通り=service_role相当)で別人名義のツイートをDB直挿入→DBには2人分あるのに、自分でログインしたアプリには自分の1件しか出ない＝RLS分離を実物で確認。見えない＝削除もできない。削除ボタン(.delete().eq('id',..))とログアウト(signOut)も実装、ログアウトするとauth.uid()が消えて一覧が空になるのも確認。

---

## 結果報告（もくもく会共有用 / 週次レポート形式）

### もともとの課題（背景 / なぜやったか）
- これまでは「こんなものを作りたい」という**ざっくりした依頼をAIに投げて作らせていた**が、**思った通りに作れず**、自分のプロジェクトが進まなかった。
- 他の人の投稿を見て、**「全体ゴール設計→フェーズ分け→標準を参考に→自分も手を動かす」**というやり方を学び、今回それを実践。
- → 今日の学びは、この **「AI任せだと理想通りに作れない／プロジェクトが進まない」課題への答え**。

### 進捗（どこまで進んだか）
- HTML1枚＋Supabaseで、Googleログイン → ツイート保存/表示 → RLSで「自分のものは自分だけ」→ 公開タイムライン → フォロー → ホームタイムラインまで実装（削除/ログアウト/下書き/フォロー/ホーム）。
- 教材は付録A・B＋第1〜12章を全部読了（第5章API・第9章キャッシュ・第10章セキュリティ点検・第11章Vibe coding・第12章スケールは概念中心）。実装は第8章まで動くものを作り、第10章では自作アプリをセキュリティ点検。
- migrations/ に今日のSQLを001〜004＋READMEで整理（再現可能なレシピ化）。
- 開発環境ゼロから構築：Supabaseプロジェクト作成、Google認証(OAuth)設定、VS Code導入、python3で簡易サーバー起動。

### 学び（何を理解したか）
- 登場人物：ブラウザ ⇄ supabase-js(窓口/API) ⇄ Supabase(認証＋DB)。
- ログイン＝OAuth（アプリがGoogleに本人確認を頼む）。鍵は anon(公開OK)と service_role(絶対秘密)の2種類。
- 保存=insert / 表示=select+order。id・created_atはDBが自動。
- 【本命】RLS＝DBが「行ごとに触っていい人か」を判定。user_id＋auth.uid()=user_idのポリシーで分離。フロントで隠すだけでは穴、止めるのはDB側。
- ハマり実体験：Google鍵の不一致(Unable to exchange external code)、コードの貼り場所ミス、RLS有効化で全ブロック→ポリシーで解放。

### 次にやること（次週TODO）
- プロフィール編集は専用の「Edit profile」画面/モーダルで操作する形に（インライン常時表示でなく本物Xのように。本人要望）。
- サンプルユーザーを数人作って「ユーザーを探す」を賑やかに。
- 画面を分ける（ホーム/プロフィール/検索ページ＋ナビ）＝本物Xの画面構成。
- AIアシスタント（Grok的）＝Claude APIキー準備後。
- 第9章 キャッシュ / 第10章 ありがちな失敗 / 第11章 Vibe coding / 第12章 スケール。
- 投稿者の表示（いまbody/UIDだけ）→ profilesテーブル等で「誰の投稿か」を名前で出す（N+1に注意）。
- セッションが再読み込みで切れる件（毎回未ログインに戻る）を調べる＝localStorage/detectSessionInUrlまわり。
- migrations を試す：今日手で打ったSQLを 001_*.sql… と順序保存して、DBを再現できる形に（実務の積み上げ方）。
- 横展開：RLSの所有モデルを電脳タグ/電脳名刺「自分のレイヤーは自分が所有」に応用（CROSS-PROJECT-NOTES参照）。
